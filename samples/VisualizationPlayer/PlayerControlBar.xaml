<UserControl
    x:Class="VisualizationPlayer.PlayerControlBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VisualizationPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:a="using:AudioVisualizer"
    mc:Ignorable="d"
    d:DesignHeight="80"
    d:DesignWidth="400">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
        <a:CustomVisualizer x:Name="positionDisplay"
                            Width="75"
                            Height="40"
                            Draw="CustomVisualizer_Draw" />
        <AppBarButton Icon="Play" Label="Play"/>
        <AppBarButton Icon="Pause" Label="Pause"/>
    </StackPanel>
</UserControl>
